<!DOCTYPE html>
<html lang="en">
<head>
    <title>Geschichte Vernetzt Report</title>
</head>
<body>
    <ul id="navigation">
    {% for item in navigation %}
        <li><a href="{{ item.href }}">{{ item.caption }}</a></li>
    {% endfor %}
    </ul>

    <h3>Answer Report for user {{username}} generated {{day}}/{{month}}/{{year}} {{hour}}:{{minute}}</h3>


    {# a comment #}
</body>
</html>