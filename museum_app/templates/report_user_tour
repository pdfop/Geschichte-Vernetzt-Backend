<!DOCTYPE html>
<html lang="en">
<head>
    <title>Geschichte Vernetzt Report</title>
<style>
table, th, tr, td {
  border:1px solid black;
}
table {
  border-collapse: collapse;
  width:100%;
}
th {
  height:20px;
  text-align: center;
}
td {
  vertical-align: top;
  height:50px;
  text-align: left;
}
</style>
</head>
<body>
    <ul id="navigation">
    {% for item in navigation %}
        <li><a href="{{ item.href }}">{{ item.caption }}</a></li>
    {% endfor %}
    </ul>
    <h3>Answer Report for user {{target}} generated {{day}}/{{month}}/{{year}} {{hour}}:{{minute}} by {{username}}</h3>
    <h4> Tour {{tourname}} by {{ownername}} </h4>
    <table>
     <tr>
        <th>Question</th>
        <th>Answer</th>
        <th>Correct Answer</th>
     </tr>
     {% for answer in answers %}
        <tr>
            <td> {{answer[0]}}
            <td> {{answer[1]}}
            <td> {{answer[2]}}
         </tr>
     {% endfor %}
     </table>


</body>
</html>