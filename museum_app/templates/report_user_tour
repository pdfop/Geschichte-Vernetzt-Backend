<!DOCTYPE html>
<html lang="en">
<head>
    <title>Geschichte Vernetzt Report</title>
<style>
table, th, tr, td {
  border:1px solid black;
}
table {
  border-collapse: collapse;
  width:100%;
}
th {
  height:20px;
  text-align: center;
}
td {
  vertical-align: top;
  height:50px;
  text-align: left;
}
</style>
</head>
<body>
    <ul id="navigation">
    {% for item in navigation %}
        <li><a href="{{ item.href }}">{{ item.caption }}</a></li>
    {% endfor %}
    </ul>
    <h3> Antworten von Benutzer {{target}} </h3>
    <h4> Fragen aus dem Rundgang {{tourname}} von {{ownername}} </h4>
    <h4>generiert am {{day}}/{{month}}/{{year}} um {{hour}}:{{minute}} von {{username}}</h4>
    <table>
     <tr>
        <th>Frage</th>
        <th>Bezugsobjekt</th>
        <th>Antwort</th>
        <th>Richtige Antwort</th>
     </tr>
     {% for answer in answers %}
        <tr>
            <td> {{answer[0]}}
            <td> {{answer[1]}}
            <td> {{answer[2]}}
            <td> {{answer[3]}}
         </tr>
     {% endfor %}
     </table>


</body>
</html>